# Netlify redirects file for SPA routing
# This ensures that all routes are handled by index.html for client-side routing

# API routes should proxy to your backend (adjust the URL as needed)
/api/*  https://your-backend-url.com/api/:splat  200

# All other routes should serve index.html to allow React Router to handle them
/*    /index.html   200
